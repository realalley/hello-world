# 2026/27 全球雪场雪质预测

无需注册的静态应用，包含221个合并目的地的中英文搜索、英文拼写容错、国家筛选、详情页、2026年12月至2027年4月的月度切换、雪质报告、机构链接和微信咨询广告。

## 运行与部署

- `npm run build`：把网站文件复制到 `dist`。
- `python3 -m http.server 8080 --directory dist`：预览构建结果。
- ESA 构建命令沿用 `npm run build`，输出目录为 `dist`。
- 详情使用 `?resort=雪场ID&month=2027-01`，无需服务器重写；直接访问或刷新均可。
- `node test-snow-outlook.mjs`：验证全部目的地、月份与搜索。

## 内容与数据

`resorts.json` 是原221个目的地数据库，没有展开子雪场。
`forecast.js` 保存可审阅的区域分析框架、月份规则和资料来源；`whistler.js` 保留已确认的惠斯勒报告与月度内容。

全部221个目的地均有独立撰写的来源事实、解释和复核重点。`evidence.js` 保留原五场分析，`destination-evidence.json` 保存新增216场资料（名称、来源链接、事实、推断、观察重点、复核日期和来源方式）。`destination-evidence.js` 是构建生成的浏览器模块，请修改JSON后重新构建。

月度背景仍是共享的季节框架；目的地证据用于解释当地地形、观测与运营条件，不是221套独立数值气象模型。页面明确区分事实、推断、季节背景与缺失数据。南半球十二月至四月按夏秋处理，合并雪区保持原221条，不展开子雪场。

来源包括雪场、运营商、政府、旅游机构及官方合作通票资料。有些稳定事实在原始调研存档中采集，本版复核其内容并明确标注；不能把版本日期当作每个网页的发布时间或实时雪况时间。历史公告不代表本季现状，规划项目不等于已投运。每个目的地证据的深度不同，均未完成历史回测，不发布虚构概率或雪量。

二世古历史图保留官方回顾列出的三个连续雪季Grand Hirafu中山测点累计降雪（1589/1450/1400cm）。它不是长期均值，也不是ENSO因果或回测结果。其他目的地缺失的长期月度序列不补造。

## 发布验证

`node test-snow-outlook.mjs` 验证221个ID、1105个月份情景、216份新增事实/推断/复核项的唯一性、来源引用与南半球隔离。运行 `npm run build` 后再发布源文件；ESA从 `dist` 提供静态网站。版本：2026-09-12。不会自动监控天气，内容更新需要人工复核和重新构建。

旧滑雪搭子代码不参与默认构建。本应用无需后端、账号、数据库连接或API密钥。二维码为用户提供的微信咨询图片。
